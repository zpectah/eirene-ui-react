import React from 'react';
import { View } from '../../../../Layout';
import { Article, Section } from '../../../../components';

const LinkView = () => {
  return (
    <View title="Link">
      <Article>
        <Section title="Link default">...Section default...</Section>
      </Article>
    </View>
  );
};

export default LinkView;
